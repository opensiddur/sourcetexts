# Printed page 43 (Hebrew; scan 68, leaf 67)

Running head `ברכות השחר`, page number `43`. Rules `ז` through `יב` of Rabbi Ishmael's
thirteen, one to a line, numbered `ז)` as on page 41.

84 words, no heading, no rubric, no citation. The words are in `hebrew/43.txt`.

## The footnote continues without a lemma

The `ILLUSTRATIONS` list begun under page 41 resumes here at item **`7.`** — under a rule,
with no lemma and no mark, picking up its own numbering across the page turn. So a numbered
list inside a commentary note runs across pages and keeps counting; the note is one note
spread over several feet, not a note per page.

## The comparison

Four differences over 84 words, every one qamats qatan, recorded by
`compare --settle-qamats-qatan`.
